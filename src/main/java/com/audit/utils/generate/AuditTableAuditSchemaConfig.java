package com.audit.utils.generate;

public class AuditTableAuditSchemaConfig {

  // TODO: move to AuditConfig once per-table config is wired through the API
  public static final String AUDIT_SCHEMA = "audit";

  private AuditTableAuditSchemaConfig() {}
}
